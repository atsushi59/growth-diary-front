# フロントから参照する環境変数（Vite は VITE_ 接頭辞のみ公開）
# ローカル開発用のバックエンド API ベースURL
VITE_API_BASE_URL=http://localhost:3000

# Amazon Cognito（#68 でデプロイ済みプールの値を設定。region は Pool ID から導出されるため不要）
VITE_COGNITO_USER_POOL_ID=
VITE_COGNITO_USER_POOL_CLIENT_ID=
